Name: stackSpace

Type: function

Syntax: the stackSpace

Syntax: stackSpace()

Summary:
Does not <return> a meaningful <value> and is included in <HyperXTalk> for
compatibility with imported <HyperCard> stacks.

Introduced: 1.0

Platforms: desktop, server

Description:
In HyperCard, the <stackSpace> function <return(glossary)|returns> the
disk size of the <current stack>. In HyperXTalk, this <function> does not
<return(constant)> a meaningful <value>.

References: return (constant), function (control structure),
heapSpace (function), hasMemory (function), value (function),
diskSpace (function), current stack (glossary), HyperXTalk (glossary),
HyperCard (glossary), return (glossary)

