Name: loadedExtensions

Type: function

Syntax: the loadedExtensions

Syntax: loadedExtensions()

Summary:
<return|Returns> a list of the currently loaded extensions.

Introduced: 1.0.0

OS: mac, windows, linux

Platforms: desktop, server

Example:
put the loadedExtensions

Example:
if "com.livecodeextensions.HyperXTalk.switchButton" is among the lines of the loadedExtensions then
    create widget as "com.livecodeextensions.HyperXTalk.switchButton"
end if    

Returns:
The <loadedExtensions> <function> <return|returns> a list of the loaded
<Builder extension|extensions>, one per <line>.

Description:
Use the <loadedExtensions> <function> to find out which 
<Builder extension|extensions> are loaded, in order to determine 
(for <library extension|libraries>) if its public handlers are available 
in the message path, or (for <widget|widgets>) if it is available to 
create with the <create widget> <command>.

Each line of the list returned by the <loadedExtensions> <function> is
the <kind> of the corresponding <HyperXTalk Builder extension|extension>.

References: create widget (command), load extension (command),
unload extension (command), function (glossary), return (glossary),
HyperXTalk Builder extension (glossary),
HyperXTalk Builder extension (glossary), command (glossary), 
line (keyword), widget (object), kind (property)

Tags: extensions

